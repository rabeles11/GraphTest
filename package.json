{
  "name": "users",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ rabeles11/users",
    "create-local": "graph create --node http://localhost:8020/ rabeles11/users",
    "remove-local": "graph remove --node http://localhost:8020/ rabeles11/users",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 rabeles11/users"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
